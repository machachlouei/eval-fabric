"""Reference / optional-dependency adapters.

Things in this package are not imported by the framework's core. Each
adapter has its own optional dependency and lives behind an extra in
``pyproject.toml``. See ADR-0005 (TraceStore) and ADR-0002 (entry points).
"""

__all__: list[str] = []
